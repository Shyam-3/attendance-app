{
  "name": "attendance-tracker-ts",
  "version": "1.0.0",
  "description": "Attendance Management System with Firebase Hosting",
  "scripts": {
    "dev:full": "npx --yes concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:frontend",
    "deploy:functions": "cd functions && npm install && firebase deploy --only functions",
    "deploy:hosting": "npm run build:frontend && firebase deploy --only hosting",
    "deploy": "cd functions && npm install && cd .. && npm run build:frontend && firebase deploy",
    "firebase:init": "firebase login && firebase init",
    "setup:functions": "cd functions && npm install",
    "setup:backend": "cd backend && npm install",
    "setup:frontend": "cd frontend && npm install",
    "setup": "npm run setup:backend && npm run setup:frontend && npm run setup:functions"
  },
  "keywords": [
    "attendance",
    "management",
    "firebase",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
