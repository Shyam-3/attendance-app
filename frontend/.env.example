# Frontend environment example
# Copy to .env and set values for your environment

# Base URL for backend API (Cloud Run, local, etc.)
VITE_API_BASE_URL=https://your-cloud-run-url.a.run.app

# Optional: enable verbose logging in the app
VITE_ENABLE_DEBUG_LOGS=true
